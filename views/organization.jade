extends layout

block content
    h1 #{org_name}
    
    p #{org_desc}
    
    h4 Members
    if locals.members
        each val in members
            li= val.username
    
    a(href="http://localhost:3000/organizations/#{org_id}/edit") Edit Information